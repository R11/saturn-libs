# Build artifacts
build/
*.o
*.elf
*.iso
*.cue
*.map
*.raw

# Host-side unit test binaries
tests/host/test_*
!tests/host/test_*.c

# Python
__pycache__/
*.pyc
.pytest_cache/

# clangd / language server (per-developer; regenerate via
# `bear --output compile_commands.json -- make -C tests/host all`)
compile_commands.json
.cache/
